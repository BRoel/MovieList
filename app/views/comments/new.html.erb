<h3>Movies:</h3>

<%= form_for(@comment) do |f| %> 
    
    <%= f.label :movie %>
    <%= f.collection_select :movie_id, Movie.all, :id, :title %>
  
    <%= f.label :content %>
    <%= f.text_field :content %>
  
    <%= f.submit %>
  
<% end %>